#include<stdio.h>
int assume[9][9][9];
int sudoku[9][9]=       {{0,7,1,0,9,0,8,0,0},
		                 {0,0,0,3,0,6,0,0,0},
				         {4,9,0,0,0,0,7,0,5},
		                 {0,1,0,9,0,0,0,0,0},
		                 {9,0,2,0,0,0,6,0,3},
		                 {0,0,0,0,0,8,0,2,0},
		                 {8,0,5,0,0,0,0,7,6},
                         {0,0,0,6,0,7,0,0,0},
	       		         {0,0,7,0,4,0,3,5,0}};
int sudokusolver(int count)
{
    if(count!=0)
     {
      int num,i,j;
      for(i=0;i<9;i++)
      {
       for(j=0;j<9;j++)
        {
         int k=0,val=0;
         if(sudoku[i][j]==0)
         {
          for(num=1;num<10;num++)
          {
           int l;
           for(l=0;l<9;l++)
           {
            if(num==sudoku[i][l])
            val++;
           }
           int boxi,boxj;
           boxi=i/3;
           boxj=j/3;
           int s,t;
           for(s=boxi*3;s=(boxi*3)+3;s++)
           {
            for(t=boxj*3;t=(boxj*3)+3;t++)
              {
               if(num==sudoku[s][t])
                val++;
              }
           }
          if(val==1)
          {
           sudoku[i][j]=num;
        count--;
          }
          else
           sudokusolver(count);
         }
}
}}
}
return 0;
}


int main()
{
int i,j,count=0;
for(i=0;i<9;i++)
{
for(j=0;j<9;j++)
{
scanf("%d",&sudoku[i][j]);
if(sudoku[i,j]==0)
count++;
}
}
sudokusolver(count);
for(i=0;i<9;i++)
{
for(j=0;j<9;j++)
{
printf("%d   ",sudoku[i][j]);
}
printf("\n");
}
return 0;
}
